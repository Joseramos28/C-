#include <iostream>

using namespace std;

int main() {
    int lista[] = {100, 200, 300 };
    int limite = 10;
    cout << "limite: ";
    cin >> limite;
    lista[1];
    for ( int i = 0; i < limite; i += 1) {
        cout <<  lista[i] << endl;
    }
     
}